void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomeScreen(), // Apontando para HomeScreen na nova estrutura
      debugShowCheckedModeBanner: false,
    );
  }
}
