import 'package:flutter/material.dart';
import 'package:seu_projeto/screens/home/home_store.dart';

class HomeScreen extends StatelessWidget {
  final HomeStore homeStore = HomeStore();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Me Conecta'),
      ),
      body: Center(
        child: Text('Conteúdo da tela inicial aqui'),
      ),
    );
  }
}
